{
  "name": "monday-webhook",
  "version": "0.1.0",
  "private": true,
  "description": "Small Express service that receives Monday.com webhooks and forwards them to the support-ops backend.",
  "main": "src/index.js",
  "type": "module",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
